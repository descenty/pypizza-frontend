export interface IPizza {
    id: number
    name: string
    image: string
    description: string
}

export interface IToken {
    token: string
}

export interface IUser {
    username: string
    first_name: string
    last_name: string
}