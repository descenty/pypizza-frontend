export interface IPizza {
    id: number
    name: string
    image: string
    description: string
}

export interface IToken {
    token: string
}

export interface IUser {
    phone: string
    fio: string
}